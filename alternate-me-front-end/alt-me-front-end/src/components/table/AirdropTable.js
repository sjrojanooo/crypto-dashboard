import React from 'react'; 
import {Table} from 'react-bootstrap';  

const AirdropTable = (props) => {

    const myArray = props.airdrop.map(array => array)

    console.log(myArray)
    return(
        <>

        </>
    )
}

export default AirdropTable; 